"use strict";

function algorithmFiles(){
return{
"src/shared/plugin-algorithms/index.js":`"use strict";
module.exports={...require("./repository"),...require("./quality"),...require("./architecture"),...require("./release")};
`,
"src/shared/plugin-algorithms/repository.js":`"use strict";
const fs=require("node:fs"),path=require("node:path"),crypto=require("node:crypto");
const {scan}=require("../file-scanner");const {tokensRough,words,lineColumn}=require("../text-utils");const {scanSecrets}=require("../secret-redaction");
function read(file,max=2e6){try{const st=fs.statSync(file);if(st.size>max)return"";return fs.readFileSync(file,"utf8");}catch{return"";}}
function hash(s){return crypto.createHash("sha256").update(String(s)).digest("hex");}
function filesOf(input){return input.files||scan(input.root,{maxFiles:input.maxFiles||10000,signal:input.signal}).filter(x=>!x.binary);}
async function repositoryMap(input){const files=filesOf(input),byExt={},byLanguage={},manifests=[];const lang={".js":"JavaScript",".ts":"TypeScript",".tsx":"TSX",".py":"Python",".cs":"C#",".java":"Java",".go":"Go",".rs":"Rust",".json":"JSON",".md":"Markdown"};let totalBytes=0;for(const f of files){byExt[f.ext||"none"]=(byExt[f.ext||"none"]||0)+1;const l=lang[f.ext]||"Other";byLanguage[l]=(byLanguage[l]||0)+1;totalBytes+=f.size;if(["package.json","pyproject.toml","Cargo.toml","go.mod","pom.xml","build.gradle","requirements.txt"].includes(path.basename(f.path)))manifests.push(f.path);}return{fileCount:files.length,totalBytes,byExt,byLanguage,manifests,largest:[...files].sort((a,b)=>b.size-a.size).slice(0,20).map(x=>({path:x.path,size:x.size}))};}
async function contextBuilder(input){const taskWords=words(input.task),active=new Set(input.activeFiles||[]),dirty=new Set(input.dirtyFiles||[]),budget=input.tokenBudget||6000;const scored=[];for(const f of filesOf(input)){const text=read(f.absolute,input.maxFileBytes||500000);if(!text)continue;let score=0;const reasons=[];if(active.has(f.path)){score+=30;reasons.push("active");}if(dirty.has(f.path)){score+=20;reasons.push("dirty");}for(const w of taskWords){if(f.path.toLowerCase().includes(w)){score+=8;reasons.push("path:"+w);}const hits=(text.toLowerCase().match(new RegExp(w.replace(/[.*+?^${}()|[\\]\\\\]/g,"\\$&"),"g"))||[]).length;if(hits){score+=Math.min(10,hits);reasons.push("content:"+w);}}if(/test|spec/.test(f.path)&&/test|hata|bug/.test(input.task||"")){score+=7;reasons.push("test-related");}scored.push({path:f.path,score,reasons,tokens:tokensRough(text)});}scored.sort((a,b)=>b.score-a.score);const picked=[];let used=0;for(const x of scored){if(used+x.tokens>budget)continue;picked.push(x);used+=x.tokens;if(picked.length>=(input.maxContextFiles||30))break;}return{picked,usedTokens:used,budget,omitted:scored.length-picked.length};}
async function codeReview(input){const rules=[
["eval","high",/\\beval\\s*\\(/g],["new-function","high",/new\\s+Function\\s*\\(/g],["empty-catch","medium",/catch\\s*\\([^)]*\\)\\s*\\{\\s*\\}/g],["inner-html","high",/\\.innerHTML\\s*=/g],["node-integration","critical",/nodeIntegration\\s*:\\s*true/g],["context-isolation-off","critical",/contextIsolation\\s*:\\s*false/g],["sandbox-off","high",/sandbox\\s*:\\s*false/g],["web-security-off","critical",/webSecurity\\s*:\\s*false/g],["insecure-content","critical",/allowRunningInsecureContent\\s*:\\s*true/g],["sync-io","low",/\\b(?:fs\\.)?(?:readFileSync|writeFileSync|readdirSync|statSync)\\s*\\(/g],["empty-rejection","medium",/\\.catch\\s*\\(\\s*\\(?.*?\\)?\\s*=>\\s*\\{?\\s*\\}?\\s*\\)/g]
];const findings=[];for(const f of input.files||[]){const src=read(f);for(const[rule,severity,re]of rules){re.lastIndex=0;let m;while((m=re.exec(src))){const pos=lineColumn(src,m.index),lines=src.split(/\\r?\\n/);findings.push({file:f,rule,severity,...pos,snippet:(lines[pos.line-1]||"").trim().slice(0,240),confidence:rule==="sync-io"?.7:.92});if(!m[0].length)re.lastIndex++;}}}return{findings,summary:findings.reduce((a,x)=>(a[x.severity]=(a[x.severity]||0)+1,a),{})};}
function symbols(src){const out=new Set();for(const re of[/export\\s+(?:async\\s+)?function\\s+(\\w+)/g,/export\\s+class\\s+(\\w+)/g,/export\\s+const\\s+(\\w+)/g,/(?:module\\.exports|exports)\\.(\\w+)\\s*=/g,/module\\.exports\\s*=\\s*\\{([^}]+)\\}/g,/(?:const|let)\\s+(\\w+)\\s*=\\s*(?:async\\s*)?\\([^)]*\\)\\s*=>/g]){let m;while((m=re.exec(src))){if(re.source.includes("\\{([^}]+)"))m[1].split(",").map(x=>x.split(":")[0].trim()).filter(Boolean).forEach(x=>out.add(x));else out.add(m[1]);}}return[...out];}
async function testGenerator(input){const results=[];for(const f of input.files||[]){const src=read(f);const syms=symbols(src);const framework=/from ["']vitest|describe\\(/.test(src)?"vitest":/jest|expect\\(/.test(src)?"jest":/pytest/.test(src)?"pytest":/node:test/.test(src)?"node:test":input.framework||"node:test";results.push({file:f,framework,symbols:syms,importSuggestion:framework==="pytest"?"from module import symbol":"const { symbol } = require('./module')",cases:syms.flatMap(s=>[{symbol:s,kind:"happy",title:s+" geçerli girdide çalışır"},{symbol:s,kind:"boundary",title:s+" sınır girdisini yönetir"},{symbol:s,kind:"error",title:s+" geçersiz girdiyi reddeder"},{symbol:s,kind:"security",title:s+" zararlı girdiyi sınırlar"}])});}return{results};}
async function bugFixPlanner(input){const text=[input.stack,input.log,input.diff].filter(Boolean).join("\\n"),candidates=[];if(/ENOENT/.test(text))candidates.push({cause:"dosya yolu veya cwd",confidence:.9});if(/EACCES|EPERM/.test(text))candidates.push({cause:"izin veya kilit",confidence:.9});if(/Cannot read propert|undefined/.test(text))candidates.push({cause:"null/undefined guard",confidence:.82});if(/npm warn deprecated/i.test(text)&&!/(ERR!|exit code [1-9])/i.test(text))candidates.push({cause:"uyarı hata sanılıyor",confidence:.94});if(/out of memory|exit code 137/i.test(text))candidates.push({cause:"bellek tükenmesi",confidence:.95});return{reproduction:input.repro||["Aynı komutu temiz klasörde çalıştır","Exit code ve ilk gerçek error satırını kaydet","Önce/sonra dosya hashlerini karşılaştır"],rootCauses:candidates.length?candidates:[{cause:"kanıt yetersiz; hedefli log gerekli",confidence:.3}],minimalFix:"En dar davranış değişikliğini uygula; önce başarısız regresyon testi yaz.",targetTests:["regression","fault-injection","rollback"],rollback:"Checkpoint/backup hash doğrulamasıyla önceki sürüme dön."};}
async function dependencyAuditor(input){const pkgPath=path.join(input.root,"package.json");let pkg={};try{pkg=JSON.parse(fs.readFileSync(pkgPath,"utf8"));}catch{return{issues:[{severity:"high",code:"PACKAGE_MISSING"}],dependencies:[]};}const locks=["package-lock.json","pnpm-lock.yaml","yarn.lock","bun.lock","bun.lockb"].filter(x=>fs.existsSync(path.join(input.root,x))),issues=[];if(locks.length!==1)issues.push({severity:"medium",code:"LOCKFILE_COUNT",locks});const deps={...(pkg.dependencies||{}),...(pkg.devDependencies||{})};for(const[name,v0]of Object.entries(deps)){const v=String(v0);if(v==="*"||v==="latest")issues.push({severity:"high",code:"UNPINNED",package:name,version:v});if(/^(git|github:|https?:)/.test(v))issues.push({severity:"medium",code:"REMOTE_DEP",package:name,version:v});if(/electron|node-gyp|better-sqlite3|sharp/.test(name))issues.push({severity:"info",code:"NATIVE_ABI",package:name});}return{dependencies:Object.entries(deps).map(([name,version])=>({name,version})),locks,issues};}
async function documentationGenerator(input){let pkg={};try{pkg=JSON.parse(fs.readFileSync(path.join(input.root,"package.json"),"utf8"));}catch{}const files=scan(input.root,{maxFiles:5000}),has=n=>files.some(f=>f.path===n||f.path.endsWith("/"+n)),scripts=pkg.scripts||{};const sections=["# "+(pkg.name||path.basename(input.root)),pkg.description||"","## Kurulum","```bat","npm install","```","## Komutlar",...Object.entries(scripts).map(([k,v])=>`- npm run ${k}: ${v}`),"## Algılanan Yapı",`- Electron: ${!!(pkg.dependencies?.electron||pkg.devDependencies?.electron)}`,`- Preload: ${has("preload.js")}`,`- Test: ${!!scripts.test}`];return{markdown:sections.join("\\n"),detected:{electron:!!(pkg.dependencies?.electron||pkg.devDependencies?.electron),preload:has("preload.js"),tests:!!scripts.test}};}
async function changelogBuilder(input){const groups={Added:[],Changed:[],Fixed:[],Security:[],Performance:[],Migration:[],"Known Issues":[]};for(const c of input.commits||[]){const m=String(c.message||""),l=m.toLowerCase();const k=/^feat|add/.test(l)?"Added":/^fix|bug/.test(l)?"Fixed":/security|secret|xss|csp/.test(l)?"Security":/perf|optimi/.test(l)?"Performance":/migrat|breaking/.test(l)?"Migration":/known/.test(l)?"Known Issues":"Changed";groups[k].push(m);}const markdown=["## "+(input.version||"Unreleased"),...Object.entries(groups).flatMap(([k,v])=>v.length?["","### "+k,...v.map(x=>"- "+x)]:[])].join("\\n");return{groups,markdown};}
async function commitMessageAssistant(input){const d=input.diff||"";const type=/test/.test(d)?"test":/README|docs\\//i.test(d)?"docs":/fix|error|bug/i.test(d)?"fix":/package(?:-lock)?\\.json/.test(d)?"chore":"feat";const scope=input.scope?`(${input.scope})`:"";return{message:`${type}${scope}: ${String(input.subject||"değişikliği uygula").slice(0,72)}${input.requirementId?"\\n\\nRefs: "+input.requirementId:""}`};}
async function promptLibrary(input){const file=path.resolve(input.file||"prompts.json");let store={prompts:[]};try{store=JSON.parse(fs.readFileSync(file,"utf8"));}catch{}if(input.action==="list")return store;if(input.action==="add"){const body=String(input.body||""),secrets=scanSecrets(body);const item={id:input.id||crypto.randomUUID(),title:input.title||"Başlıksız",body:secrets.length?"[REDACTED]":body,tags:input.tags||[],version:1,createdAt:new Date().toISOString()};store.prompts.push(item);const tmp=file+".tmp";fs.writeFileSync(tmp,JSON.stringify(store,null,2));fs.renameSync(tmp,file);return{item,secrets:secrets.length};}if(input.action==="remove"){store.prompts=store.prompts.filter(x=>x.id!==input.id);fs.writeFileSync(file,JSON.stringify(store,null,2));return{removed:input.id};}if(input.action==="export")return{data:store};if(input.action==="import"){fs.writeFileSync(file,JSON.stringify(input.data||{prompts:[]},null,2));return{imported:true};}throw new Error("action");}
module.exports={"repository-map":repositoryMap,"context-builder":contextBuilder,"code-review":codeReview,"test-generator":testGenerator,"bug-fix-planner":bugFixPlanner,"dependency-auditor":dependencyAuditor,"documentation-generator":documentationGenerator,"changelog-builder":changelogBuilder,"commit-message-assistant":commitMessageAssistant,"prompt-library":promptLibrary};
`,
"src/shared/plugin-algorithms/quality.js":`"use strict";
const fs=require("node:fs"),path=require("node:path");const {scan}=require("../file-scanner");const {lineColumn}=require("../text-utils");const {scanSecrets,redact}=require("../secret-redaction");
function read(f,max=2e6){try{if(fs.statSync(f).size>max)return"";return fs.readFileSync(f,"utf8");}catch{return"";}}
async function fileSummarizer(input){const results=[];for(const f of input.files||[]){const src=read(f),imports=[...src.matchAll(/(?:require\\(["']([^"']+)|from\\s+["']([^"']+)|import\\s+["']([^"']+))/g)].map(m=>m[1]||m[2]||m[3]),symbols=[...src.matchAll(/(?:function|class|const|let)\\s+(\\w+)/g)].map(m=>m[1]),publicApi=[...src.matchAll(/(?:exports\\.(\\w+)|export\\s+(?:function|class|const)\\s+(\\w+))/g)].map(m=>m[1]||m[2]),todos=[...src.matchAll(/\\b(TODO|FIXME|HACK)\\b[: ]*(.*)/gi)].map(m=>({tag:m[1],text:m[2].trim()}));results.push({file:f,lines:src.split(/\\r?\\n/).length,complexity:(src.match(/\\b(if|for|while|switch|case|catch)\\b/g)||[]).length,imports:[...new Set(imports)],symbols:[...new Set(symbols)].slice(0,200),publicApi:[...new Set(publicApi)],todos});}return{results};}
async function localSearch(input){if(String(input.query||"").length>300)throw new Error("pattern uzun");if(input.regex&&/(?:\\(.+\\)){5,}|\\*\\+|\\+\\*/.test(input.query))throw new Error("karmaşık regex");const flags=input.caseSensitive?"":"i",re=input.regex?new RegExp(input.query,flags):new RegExp(String(input.query).replace(/[.*+?^${}()|[\\]\\\\]/g,"\\$&"),flags),out=[];for(const f of scan(input.root,{maxFiles:input.maxFiles||10000,signal:input.signal})){if(f.binary||f.size>(input.maxFileBytes||1e6))continue;if(input.ext?.length&&!input.ext.includes(f.ext))continue;if(input.pathIncludes&&!f.path.includes(input.pathIncludes))continue;const lines=read(f.absolute).split(/\\r?\\n/);for(let i=0;i<lines.length;i++){re.lastIndex=0;if(re.test(lines[i])){out.push({file:f.path,line:i+1,snippet:lines.slice(Math.max(0,i-(input.context||1)),i+(input.context||1)+1).join("\\n")});if(out.length>=(input.maxResults||200))return{matches:out,truncated:true};}}}return{matches:out,truncated:false};}
async function diffExplainer(input){const d=String(input.diff||""),files=[...d.matchAll(/^\\+\\+\\+ b\\/(.+)$/gm)].map(m=>m[1]),added=(d.match(/^\\+[^+]/gm)||[]).length,removed=(d.match(/^-[^-]/gm)||[]).length;return{files,added,removed,behavior:/return|throw|if\\s*\\(|switch/.test(d),security:/(innerHTML|eval|token|secret|password|webSecurity)/i.test(d),migration:/ALTER TABLE|CREATE TABLE|schema|migration/i.test(d),tests:/test|spec/i.test(d),rollback:"Base hash ve checkpoint doğrulamasıyla geri al"};}
function severity(line){return /fatal|panic|uncaught|segfault/i.test(line)?"critical":/error|failed|exception/i.test(line)?"high":/warn|deprecated/i.test(line)?"medium":"info";}
async function logAnalyzer(input){const lines=input.lines||String(input.text||"").split(/\\r?\\n/),map=new Map();for(const raw of lines){if(!raw.trim())continue;const ts=raw.match(/\\d{4}-\\d{2}-\\d{2}[T ][\\d:.+-Z]+/)?.[0]||null;let sig=raw.replace(/\\d{4}-\\d{2}-\\d{2}[T ][\\d:.+-Z]+/g,"<TS>").replace(/\\bpid[=: ]?\\d+/gi,"pid=<N>").replace(/\\b[0-9a-f]{8}-[0-9a-f-]{27,}\\b/gi,"<GUID>").replace(/0x[0-9a-f]+/gi,"<HEX>").replace(/:\\d{2,5}\\b/g,":<PORT>").replace(/([A-Za-z]:)?[\\/](?:[^\\s:]+[\\/])+([^\\/\\s:]+)/g,"<PATH>/$2");sig=sig.slice(0,300);const x=map.get(sig)||{signature:sig,count:0,firstSeen:ts,lastSeen:ts,severity:severity(raw),samples:[]};x.count++;x.lastSeen=ts||x.lastSeen;if(x.samples.length<3)x.samples.push(redact(raw).text);map.set(sig,x);}return{clusters:[...map.values()].sort((a,b)=>b.count-a.count)};}
async function apiContractReview(input){const issues=[];function schema(s,p="$",seen=new Set()){if(!s||typeof s!=="object")return;if(seen.has(s))return;seen.add(s);if(s.type==="object"){for(const r of s.required||[])if(!s.properties?.[r])issues.push({severity:"high",path:p,code:"REQUIRED_MISSING",key:r});if(s.additionalProperties===undefined)issues.push({severity:"low",path:p,code:"ADDITIONAL_UNSPECIFIED"});for(const[k,v]of Object.entries(s.properties||{}))schema(v,p+"."+k,seen);}if(s.enum&&!s.enum.length)issues.push({severity:"medium",path:p,code:"EMPTY_ENUM"});}
schema(input.schema);for(const[p,item]of Object.entries(input.openapi?.paths||{}))for(const[m,op]of Object.entries(item||{}))if(["get","post","put","patch","delete"].includes(m)&&!op.responses)issues.push({severity:"high",path:p+":"+m,code:"RESPONSES_MISSING"});if(input.before&&input.after){for(const k of input.before.required||[])if(!(input.after.required||[]).includes(k))issues.push({severity:"high",code:"BREAKING_REQUIRED_REMOVED",key:k});}if(input.ipc&&!input.ipc.requestId)issues.push({severity:"medium",code:"IPC_REQUEST_ID_MISSING"});return{issues};}
async function secretScanner(input){const findings=[];for(const f of input.files||[]){const src=read(f);scanSecrets(src).forEach(x=>findings.push({file:f,...x}));}return{findings};}
function classifyLicense(s){s=String(s||"").toUpperCase();if(/MIT|APACHE|BSD|ISC|UNLICENSE/.test(s))return"permissive";if(/LGPL|MPL|EPL/.test(s))return"weak-copyleft";if(/AGPL|GPL/.test(s))return"strong-copyleft";if(/BUSL|SSPL|ELASTIC|COMMERCIAL/.test(s))return"commercial";return"unknown";}
async function licenseScanner(input){let pkg={};try{pkg=JSON.parse(fs.readFileSync(path.join(input.root,"package.json"),"utf8"));}catch{}const deps=Object.keys({...pkg.dependencies,...pkg.devDependencies}),results=[];for(const name of deps){let lic=null;try{lic=JSON.parse(fs.readFileSync(path.join(input.root,"node_modules",name,"package.json"),"utf8")).license;}catch{}results.push({name,license:lic,category:classifyLicense(lic)});}return{results,summary:results.reduce((a,x)=>(a[x.category]=(a[x.category]||0)+1,a),disclaimer:"Hukuki tavsiye değildir."};}
async function performanceHints(input){const rules=[["sync-io",/\\b(?:readFileSync|writeFileSync|readdirSync|statSync)\\s*\\(/g],["listener",/\\.(?:on|addEventListener)\\s*\\(/g],["interval",/setInterval\\s*\\(/g],["large-ipc",/ipc(?:Main|Renderer)\\.[a-z]+\\s*\\([^)]{300,}/g],["loop-io",/(?:for|while)\\s*\\([\\s\\S]{0,300}(?:readFile|stat)\\s*\\(/g]];const findings=[];for(const f of input.files||[]){const src=read(f);for(const[id,re]of rules){re.lastIndex=0;let m;while((m=re.exec(src)))findings.push({file:f,id,...lineColumn(src,m.index)});}}return{findings};}
async function refactorPlanner(input){return{target:input.target,steps:["Baseline davranış testini sabitle","Saf yardımcıları ayır","Yan etkileri servis sınırına taşı","State ownership netleştir","IPC sözleşmesini daralt","Hedefli testleri taşı","Ölü kodu temizle"].map((description,i)=>({order:i+1,description,rollback:"önceki commit/checkpoint"})),reversible:true};}
async function workspaceDoctor(input){const root=input.root,checks=[],exists=p=>fs.existsSync(path.join(root,p));let pkg={};try{pkg=JSON.parse(fs.readFileSync(path.join(root,"package.json"),"utf8"));}catch{}const html=scan(root,{maxFiles:3000}).filter(f=>f.ext===".html").map(f=>read(f.absolute)).join("\\n"),allText=scan(root,{maxFiles:3000}).filter(f=>!f.binary&&f.size<500000).map(f=>read(f.absolute)).join("\\n");checks.push({id:"node",ok:Number(process.versions.node.split(".")[0])>=20,value:process.versions.node});checks.push({id:"package",ok:exists("package.json")});const locks=["package-lock.json","pnpm-lock.yaml","yarn.lock","bun.lock","bun.lockb"].filter(exists);checks.push({id:"single-lock",ok:locks.length===1,value:locks});checks.push({id:"electron",ok:!!(pkg.dependencies?.electron||pkg.devDependencies?.electron)});checks.push({id:"builder",ok:!!(pkg.devDependencies?.["electron-builder"]||pkg.dependencies?.["electron-builder"])});checks.push({id:"test-script",ok:!!pkg.scripts?.test});checks.push({id:"csp",ok:/Content-Security-Policy/i.test(html)});checks.push({id:"preload",ok:scan(root,{maxFiles:3000}).some(f=>/preload/i.test(f.path))});checks.push({id:"mojibake",ok:!/[ÃÄÅ�]/.test(allText)});checks.push({id:"windows-build",ok:!!(pkg.scripts?.dist||pkg.scripts?.build)});const env=["OPENAI_API_KEY","GEMINI_API_KEY","GOOGLE_API_KEY","ANTHROPIC_API_KEY","DEEPSEEK_API_KEY","KIMI_API_KEY","MOONSHOT_API_KEY","LOCAL_LLM_BASE_URL"].map(name=>({name,set:!!process.env[name]}));return{checks,env,summary:{passed:checks.filter(x=>x.ok).length,total:checks.length}};}
module.exports={"file-summarizer":fileSummarizer,"local-search":localSearch,"diff-explainer":diffExplainer,"log-analyzer":logAnalyzer,"api-contract-review":apiContractReview,"secret-scanner":secretScanner,"license-scanner":licenseScanner,"performance-hints":performanceHints,"refactor-planner":refactorPlanner,"workspace-doctor":workspaceDoctor};
`,
"src/shared/plugin-algorithms/architecture.js":`"use strict";
const fs=require("node:fs"),path=require("node:path"),crypto=require("node:crypto");const {scan}=require("../file-scanner");const {scanSecrets}=require("../secret-redaction");const {lineColumn}=require("../text-utils");
function read(f,max=2e6){try{if(fs.statSync(f).size>max)return"";return fs.readFileSync(f,"utf8");}catch{return"";}}
function imports(src,ext){const out=[];const patterns=ext===".py"?[/^\\s*(?:from\\s+([\\w.]+)\\s+import|import\\s+([\\w.]+))/gm]:ext===".cs"?[/^\\s*using\\s+([\\w.]+);/gm]:ext===".java"?[/^\\s*import\\s+([\\w.]+);/gm]:[/(?:require\\(["']([^"']+)|from\\s+["']([^"']+)|import\\s+["']([^"']+))/g];for(const re of patterns){let m;while((m=re.exec(src)))out.push(m[1]||m[2]||m[3]);}return out;}
async function architectureGraph(input){const nodes=[],edges=[];for(const f of scan(input.root,{maxFiles:input.maxFiles||10000}).filter(x=>![".json",".md"].includes(x.ext)&&!x.binary)){nodes.push({id:f.path,size:f.size});for(const i of imports(read(f.absolute),f.ext))edges.push({from:f.path,to:i});}const inMap={},outMap={};for(const e of edges){outMap[e.from]=(outMap[e.from]||0)+1;inMap[e.to]=(inMap[e.to]||0)+1;}const cycles=[];const adj={};edges.forEach(e=>(adj[e.from]??=[]).push(e.to));function walk(n,stack=[]){if(stack.includes(n)){cycles.push([...stack.slice(stack.indexOf(n)),n]);return;}if(stack.length>20)return;for(const x of adj[n]||[])if(adj[x])walk(x,[...stack,n]);}nodes.slice(0,1000).forEach(n=>walk(n.id));return{nodes,edges,cycles:[...new Set(cycles.map(JSON.stringify))].map(JSON.parse).slice(0,100),highFanIn:Object.entries(inMap).sort((a,b)=>b[1]-a[1]).slice(0,20),highFanOut:Object.entries(outMap).sort((a,b)=>b[1]-a[1]).slice(0,20)};}
async function deadCodeDetector(input){const files=scan(input.root,{maxFiles:10000}).filter(x=>!x.binary&&/[.](?:js|ts|tsx|jsx)$/.test(x.path)),provided=new Map(),used=new Set(),entries=new Set(input.entrypoints||["index.js","main.js","src/index.js","src/main.js"]);for(const f of files){const src=read(f.absolute),exp=[...src.matchAll(/(?:export\\s+(?:function|class|const)\\s+|exports\\.|module\\.exports\\.)(\\w+)/g)].map(m=>m[1]);provided.set(f.path,exp);for(const i of imports(src,f.ext))used.add(i);if(/import\\s*\\(/.test(src))f.dynamic=true;}const candidates=[];for(const[f,syms]of provided)for(const s of syms)if(!entries.has(f)&&![...used].some(x=>x.includes(path.basename(f,path.extname(f))))candidates.push({file:f,symbol:s,confidence:read(path.join(input.root,f)).includes("import(")?.35:.72,reason:"import graph'ta erişilemiyor"});return{candidates,warning:"Dinamik import ve reflection nedeniyle otomatik silme yapılmaz."};}
function tokens(src){return src.replace(/\/\*[\\s\\S]*?\*\/|\/\/.*$/gm,"").replace(/['"`][^'"`]*['"`]/g,"STR").split(/[^A-Za-z0-9_]+/).filter(Boolean);}
async function duplicateCodeFinder(input){const min=input.minLines||6,threshold=input.threshold||.8,blocks=[];for(const f of scan(input.root,{maxFiles:5000}).filter(x=>!x.binary&&x.size<1e6)){const lines=read(f.absolute).split(/\\r?\\n/);for(let i=0;i+min<=lines.length;i+=Math.max(1,Math.floor(min/2))){const raw=lines.slice(i,i+min).join("\\n"),t=tokens(raw);if(t.length<10)continue;blocks.push({file:f.path,line:i+1,set:new Set(t),hash:crypto.createHash("sha1").update(t.join(" ")).digest("hex")});}}const matches=[];for(let i=0;i<blocks.length;i++)for(let j=i+1;j<blocks.length&&j<i+500;j++){if(blocks[i].file===blocks[j].file)continue;const a=blocks[i].set,b=blocks[j].set,inter=[...a].filter(x=>b.has(x)).length,sim=inter/Math.max(a.size,b.size);if(blocks[i].hash===blocks[j].hash||sim>=threshold)matches.push({a:{file:blocks[i].file,line:blocks[i].line},b:{file:blocks[j].file,line:blocks[j].line},similarity:blocks[i].hash===blocks[j].hash?1:sim});}return{matches:matches.sort((a,b)=>b.similarity-a.similarity).slice(0,input.maxResults||200)};}
async function migrationPlanner(input){const before=input.before||{},after=input.after||{},steps=[],warnings=[];for(const k of Object.keys(after))if(!(k in before))steps.push({type:"add",path:k,preflight:"default/nullability kontrolü",rollback:"alanı kaldır"});for(const k of Object.keys(before))if(!(k in after)){steps.push({type:"remove",path:k,preflight:"kullanım ve veri sayımı",rollback:"backup'tan geri yükle"});warnings.push({severity:"high",path:k,code:"DATA_LOSS"});}for(const k of Object.keys(after))if(k in before&&JSON.stringify(before[k])!==JSON.stringify(after[k]))steps.push({type:"change",path:k,from:before[k],to:after[k],preflight:"cast/constraint kontrolü",rollback:"önceki şema"});return{engine:input.engine||"generic",steps:steps.map((x,i)=>({order:i+1,...x})),warnings,backupRequired:steps.length>0,transactionRecommended:true};}
async function ipcContractAuditor(input){const channels={main:new Map(),preload:new Map(),renderer:new Map()},findings=[];for(const f of input.files||[]){const src=read(f);for(const m of src.matchAll(/ipcMain\\.(handle|on)\\s*\\(\\s*["']([^"']+)/g))channels.main.set(m[2],{file:f,kind:m[1]});for(const m of src.matchAll(/ipcRenderer\\.(invoke|send)\\s*\\(\\s*["']([^"']+)/g))channels.renderer.set(m[2],{file:f,kind:m[1]});for(const m of src.matchAll(/(?:exposeInMainWorld|contextBridge)[\\s\\S]{0,1000}?["']([^"']+)["']/g))channels.preload.set(m[1],{file:f});if(/invoke\\s*:\\s*\\(channel/.test(src))findings.push({severity:"critical",file:f,code:"GENERIC_INVOKE"});if(/ipcMain\\.(handle|on)/.test(src)&&!/sender|webContents|frame/.test(src))findings.push({severity:"medium",file:f,code:"SENDER_VALIDATION_MISSING"});}for(const c of channels.renderer.keys())if(!channels.main.has(c))findings.push({severity:"high",channel:c,code:"ORPHAN_RENDERER"});for(const c of channels.main.keys())if(!channels.renderer.has(c))findings.push({severity:"low",channel:c,code:"UNUSED_MAIN"});return{channels:Object.fromEntries([...Object.entries(channels)].map(([k,v])=>[k,Object.fromEntries(v)])),findings};}
async function electronSecurityAuditor(input){const rules=[["NODE_INTEGRATION","critical",/nodeIntegration\\s*:\\s*true/g],["CONTEXT_ISOLATION","critical",/contextIsolation\\s*:\\s*false/g],["SANDBOX","high",/sandbox\\s*:\\s*false/g],["WEB_SECURITY","critical",/webSecurity\\s*:\\s*false/g],["INSECURE_CONTENT","critical",/allowRunningInsecureContent\\s*:\\s*true/g],["REMOTE_MODULE","high",/(?:enableRemoteModule|@electron\\/remote)/g]],findings=[];let all="";for(const f of input.files||[]){const src=read(f);all+=src;for(const[c,s,re]of rules){re.lastIndex=0;let m;while((m=re.exec(src)))findings.push({file:f,code:c,severity:s,...lineColumn(src,m.index)});}}if(!/setWindowOpenHandler/.test(all))findings.push({code:"WINDOW_OPEN_POLICY",severity:"medium"});if(!/will-navigate/.test(all))findings.push({code:"NAVIGATION_POLICY",severity:"medium"});if(!/setPermissionRequestHandler/.test(all))findings.push({code:"PERMISSION_HANDLER",severity:"medium"});if(!/Content-Security-Policy/.test(all))findings.push({code:"CSP_MISSING",severity:"high"});return{findings};}
async function buildLogDiagnoser(input){const lines=input.lines||String(input.text||"").split(/\\r?\\n/),events=[];for(let i=0;i<lines.length;i++){const l=lines[i];if(/npm warn deprecated/i.test(l))events.push({line:i+1,type:"warning",code:"DEPRECATED",text:l});else if(/(?:npm ERR!|BUILD HATASI|fatal error|error MSB|makensis.*error)/i.test(l))events.push({line:i+1,type:"fatal",code:"BUILD_ERROR",text:l,context:lines.slice(Math.max(0,i-3),i+4)});else if(/exit code[: ]+[1-9]/i.test(l))events.push({line:i+1,type:"fatal",code:"NONZERO_EXIT",text:l});}const fatal=events.filter(x=>x.type==="fatal");return{success:fatal.length===0&&(input.exitCode??0)===0,exitCode:input.exitCode??0,events,rootCause:fatal[0]||null,warningCount:events.length-fatal.length};}
async function encodingMojibakeDoctor(input){const findings=[];for(const f of input.files||[]){const b=fs.readFileSync(f),src=b.toString("utf8");if(src.includes("�"))findings.push({file:f,code:"REPLACEMENT_CHAR",count:(src.match(/�/g)||[]).length});for(const [bad,good]of[["Ã§","ç"],["Ä±","ı"],["ÅŸ","ş"],["Ã¶","ö"],["Ã¼","ü"],["ÄŸ","ğ"],["Ä°","İ"]])if(src.includes(bad))findings.push({file:f,code:"MOJIBAKE",bad,proposed:good,count:src.split(bad).length-1});if(b[0]===0xef&&b[1]===0xbb&&b[2]===0xbf)findings.push({file:f,code:"UTF8_BOM"});}return{findings,dryRun:true,advice:"Önce backup/hash al; yalnız doğrulanan eşleşmeleri UTF-8 olarak yeniden yaz."};}
async function configDriftDetector(input){const sets={defaults:new Set(Object.keys(input.defaults||{})),schema:new Set(Object.keys(input.schema?.properties||{})),env:new Set(input.envKeys||[]),docs:new Set(input.docKeys||[])},all=new Set(Object.values(sets).flatMap(x=>[...x])),drift=[];for(const k of all){const present=Object.fromEntries(Object.entries(sets).map(([n,s])=>[n,s.has(k)]));if(new Set(Object.values(present)).size>1)drift.push({key:k,present});}return{drift};}
async function environmentKeyDoctor(input){const official=["OPENAI_API_KEY","GEMINI_API_KEY","GOOGLE_API_KEY","ANTHROPIC_API_KEY","DEEPSEEK_API_KEY","KIMI_API_KEY","MOONSHOT_API_KEY","LOCAL_LLM_BASE_URL"],aliases={OPENAL_API_KEY:"OPENAI_API_KEY",OPENAI_API_KEYY:"OPENAI_API_KEY",GEMINI_API_KEYY:"GEMINI_API_KEY"},env=input.env||process.env;return{official:official.map(name=>({name,set:!!env[name]})),typos:Object.entries(aliases).filter(([bad])=>env[bad]).map(([bad,target])=>({bad,target,migration:`${bad} değerini ${target} adına taşı`}))};}
module.exports={"architecture-graph":architectureGraph,"dead-code-detector":deadCodeDetector,"duplicate-code-finder":duplicateCodeFinder,"migration-planner":migrationPlanner,"ipc-contract-auditor":ipcContractAuditor,"electron-security-auditor":electronSecurityAuditor,"build-log-diagnoser":buildLogDiagnoser,"encoding-mojibake-doctor":encodingMojibakeDoctor,"config-drift-detector":configDriftDetector,"environment-key-doctor":environmentKeyDoctor};
`,
"src/shared/plugin-algorithms/release.js":`"use strict";
const fs=require("node:fs"),path=require("node:path"),crypto=require("node:crypto");const {scan}=require("../file-scanner");const {scanSecrets}=require("../secret-redaction");const {lineColumn}=require("../text-utils");
function read(f,max=2e6){try{if(fs.statSync(f).size>max)return"";return fs.readFileSync(f,"utf8");}catch{return"";}}
async function gitRiskAnalyzer(input){const risks=[];if(input.dirty?.length)risks.push({severity:"medium",code:"DIRTY",count:input.dirty.length});if(input.untracked?.length)risks.push({severity:"medium",code:"UNTRACKED",count:input.untracked.length});if((input.diff||"").length>(input.maxDiffBytes||500000))risks.push({severity:"high",code:"LARGE_DIFF"});if(/GIT binary patch|Binary files/.test(input.diff||""))risks.push({severity:"medium",code:"BINARY_DIFF"});if(scanSecrets(input.diff||"").length)risks.push({severity:"critical",code:"SECRET_IN_DIFF"});if(/package-lock|pnpm-lock|yarn.lock/.test(input.diff||""))risks.push({severity:"info",code:"LOCKFILE_CHANGED"});if(/migration|ALTER TABLE|CREATE TABLE/i.test(input.diff||""))risks.push({severity:"high",code:"MIGRATION_CHANGED"});return{risks,verdict:risks.some(x=>x.severity==="critical")?"block":risks.some(x=>x.severity==="high")?"review":"pass"};}
async function releaseReadiness(input){const gates=(input.gates||[]).map(g=>({id:g.id,ok:!!g.ok,required:g.required!==false,evidence:g.evidence||null})),failed=gates.filter(x=>x.required&&!x.ok);return{ready:failed.length===0,gates,failed,summary:`${gates.length-failed.length}/${gates.length} kapı geçti`};}
async function testGapAnalyzer(input){const sourceSymbols=new Set(input.sourceSymbols||[]),testText=(input.testTexts||[]).join("\\n"),gaps=[];for(const s of sourceSymbols)if(!new RegExp(`\\b${String(s).replace(/[.*+?^${}()|[\\]\\\\]/g,"\\$&")}\\b`).test(testText))gaps.push({symbol:s,confidence:input.coverage?.[s]===0?.98:.65,reason:input.coverage?.[s]===0?"coverage 0":"test metninde referans yok"});return{gaps,covered:[...sourceSymbols].length-gaps.length};}
async function flakyTestAnalyzer(input){const byTest=new Map();for(const r of input.runs||[]){for(const t of r.tests||[]){const x=byTest.get(t.name)||{name:t.name,pass:0,fail:0,durations:[],messages:[]};t.ok?x.pass++:x.fail++;x.durations.push(t.durationMs||0);if(t.message)x.messages.push(t.message);byTest.set(t.name,x);}}const findings=[];for(const x of byTest.values()){if(x.pass&&x.fail)findings.push({test:x.name,code:"NONDETERMINISTIC_RESULT",severity:"high",pass:x.pass,fail:x.fail});const avg=x.durations.reduce((a,b)=>a+b,0)/Math.max(1,x.durations.length),max=Math.max(0,...x.durations);if(max>avg*3&&max>1000)findings.push({test:x.name,code:"TIMING_VARIANCE",severity:"medium",avg,max});const msg=x.messages.join(" ");if(/EADDRINUSE|port/i.test(msg))findings.push({test:x.name,code:"PORT_COLLISION",severity:"high"});if(/ENOENT|temp|tmp/i.test(msg))findings.push({test:x.name,code:"FILE_COLLISION",severity:"medium"});}return{findings};}
async function errorHandlingAuditor(input){const findings=[],rules=[["EMPTY_CATCH",/catch\\s*\\([^)]*\\)\\s*\\{\\s*\\}/g],["GENERIC_CATCH",/catch\\s*\\([^)]*\\)\\s*\\{[^}]{0,300}(?:console\\.log|return false|return null)[^}]*\\}/g],["LOST_CAUSE",/throw\\s+new\\s+Error\\s*\\([^)]*\\)(?![^\\n]*cause)/g],["UNHANDLED_PROMISE",/\\b(?:fetch|readFile|writeFile|send)\\([^;]+;(?![^\\n]*(?:await|catch|then))/g],["MISSING_FINALLY",/try\\s*\\{[\\s\\S]{0,500}(?:open|lock|acquire)[\\s\\S]{0,500}catch(?![\\s\\S]{0,300}finally)/g]];for(const f of input.files||[]){const src=read(f);for(const[c,re]of rules){re.lastIndex=0;let m;while((m=re.exec(src)))findings.push({file:f,code:c,...lineColumn(src,m.index)});}}return{findings};}
async function accessibilityUiAuditor(input){const findings=[];for(const f of input.files||[]){const src=read(f);for(const m of src.matchAll(/<img\\b(?![^>]*\\balt=)[^>]*>/gi))findings.push({file:f,code:"IMG_ALT",severity:"high",...lineColumn(src,m.index)});for(const m of src.matchAll(/<(?:button|a)\\b[^>]*>\\s*<[^>]+>\\s*<\\/(?:button|a)>/gi))findings.push({file:f,code:"ICON_NAME",severity:"medium",...lineColumn(src,m.index)});for(const m of src.matchAll(/<(div|span)\\b[^>]*onClick=/gi))if(!/role=|tabIndex=|onKey/.test(m[0]))findings.push({file:f,code:"CLICK_NO_KEYBOARD",severity:"high",...lineColumn(src,m.index)});for(const m of src.matchAll(/aria-[a-z-]+=/gi))if(!/aria-(label|labelledby|describedby|hidden|expanded|controls|current|live|pressed|selected|checked|disabled)/i.test(m[0]))findings.push({file:f,code:"ARIA_SUSPECT",severity:"low",...lineColumn(src,m.index)});}return{findings};}
function placeholders(s){return[...String(s).matchAll(/\\{\\{?([A-Za-z0-9_]+)\\}?\\}|%[sd]|\\$\\{([A-Za-z0-9_]+)\\}/g)].map(m=>m[1]||m[2]||m[0]).sort();}
async function localizationIntegrity(input){const locales=input.locales||{},all=new Set(Object.values(locales).flatMap(x=>Object.keys(x))),findings=[];for(const key of all){for(const[lang,obj]of Object.entries(locales))if(!(key in obj))findings.push({code:"MISSING_KEY",lang,key,severity:"high"});const vals=Object.entries(locales).filter(([,o])=>key in o).map(([lang,o])=>[lang,String(o[key])]);if(vals.length>1){const base=placeholders(vals[0][1]).join("|");for(const[lang,v]of vals.slice(1))if(placeholders(v).join("|")!==base)findings.push({code:"PLACEHOLDER_MISMATCH",lang,key,severity:"high"});for(const[lang,v]of vals)if(/[ÃÄÅ�]/.test(v))findings.push({code:"MOJIBAKE",lang,key,severity:"high"});}}return{findings};}
async function workspaceIndexer(input){const previous=input.previous||{generation:0,files:{}},generation=(input.generation??previous.generation+1),manifest={generation,createdAt:Date.now(),files:{}};for(const f of scan(input.root,{maxFiles:input.maxFiles||20000,signal:input.signal}).filter(x=>!x.binary)){const src=read(f.absolute,input.maxFileBytes||1e6),h=crypto.createHash("sha256").update(src).digest("hex");if(previous.files?.[f.path]?.hash===h)manifest.files[f.path]=previous.files[f.path];else manifest.files[f.path]={hash:h,size:f.size,mtimeMs:f.mtimeMs,symbols:[...src.matchAll(/(?:function|class|const|let)\\s+(\\w+)/g)].map(m=>m[1]).slice(0,200),imports:[...src.matchAll(/(?:require\\(["']([^"']+)|from\\s+["']([^"']+))/g)].map(m=>m[1]||m[2]).slice(0,200)};}if(input.latestGeneration!=null&&generation!==input.latestGeneration)return{stale:true,generation};return{stale:false,manifest,changed:Object.keys(manifest.files).filter(k=>manifest.files[k]!==previous.files?.[k])};}
function semver(v){const m=String(v||"0.0.0").match(/(\\d+)\\.(\\d+)\\.(\\d+)/);return m?m.slice(1).map(Number):[0,0,0];}
async function dependencyUpgradePlanner(input){const a=semver(input.current),b=semver(input.target),risks=[];if(b[0]>a[0])risks.push({severity:"high",code:"MAJOR"});if(input.peerConflicts?.length)risks.push({severity:"high",code:"PEER",items:input.peerConflicts});if(input.nativeElectron)risks.push({severity:"high",code:"ELECTRON_ABI"});return{risks,steps:["Lockfile ve baseline testleri kaydet","Paketi tek başına yükselt","Peer dependency kontrolü","Native modülleri rebuild et","Unit/integration/E2E çalıştır","Build artifact smoke test","Başarısızsa lockfile ve package değişikliklerini geri al"].map((x,i)=>({order:i+1,description:x})),rollback:"package ve lockfile checkpoint"};}
function parsePatch(d){const files=[];let current=null;for(const line of String(d||"").split(/\\r?\\n/)){if(line.startsWith("+++ b/")){current={path:line.slice(6),added:[],removed:[]};files.push(current);}else if(current&&line.startsWith("+")&&!line.startsWith("+++"))current.added.push(line.slice(1));else if(current&&line.startsWith("-")&&!line.startsWith("---"))current.removed.push(line.slice(1));}return files;}
async function patchSafetyValidator(input){const files=parsePatch(input.diff),findings=[];for(const f of files){if(f.path.includes("..")||path.isAbsolute(f.path))findings.push({severity:"critical",code:"PATH_ESCAPE",file:f.path});if(/^(?:\.git|node_modules|release|dist)\//.test(f.path))findings.push({severity:"high",code:"FORBIDDEN_PATH",file:f.path});const add=f.added.join("\\n");if(scanSecrets(add).length)findings.push({severity:"critical",code:"SECRET_ADDED",file:f.path});if(Buffer.byteLength(add)>(input.maxFilePatchBytes||1e6))findings.push({severity:"high",code:"LARGE_PATCH",file:f.path});if(/GIT binary patch/.test(input.diff||""))findings.push({severity:"high",code:"BINARY_PATCH",file:f.path});}if(input.expectedBaseHash&&input.actualBaseHash!==input.expectedBaseHash)findings.push({severity:"critical",code:"BASE_HASH_MISMATCH"});return{files:files.map(x=>x.path),findings,applyReady:!findings.some(x=>["critical","high"].includes(x.severity)),testImpact:files.some(f=>/src|electron|main|preload/.test(f.path))?"integration tests required":"targeted tests"};}
module.exports={"git-risk-analyzer":gitRiskAnalyzer,"release-readiness":releaseReadiness,"test-gap-analyzer":testGapAnalyzer,"flaky-test-analyzer":flakyTestAnalyzer,"error-handling-auditor":errorHandlingAuditor,"accessibility-ui-auditor":accessibilityUiAuditor,"localization-integrity":localizationIntegrity,"workspace-indexer":workspaceIndexer,"dependency-upgrade-planner":dependencyUpgradePlanner,"patch-safety-validator":patchSafetyValidator};
`
};}
module.exports={algorithmFiles};
